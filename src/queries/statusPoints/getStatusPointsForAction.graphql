query getStatusPointsForAction($partnerId: String!, $tags: [String!]!) {
    getStatusPointsForAction: get_status_points_for_action(
        partner_id: $partnerId
        tags: $tags
    ) {
        result
    }
}
