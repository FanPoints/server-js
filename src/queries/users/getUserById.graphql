query get_user_by_id($project_id: String!, $user_id: String!) {
    get_user_by_id(project_id: $project_id, user_id: $user_id) {
        unknown_user_error {
            _empty
        }
        user {
            mail_address
            user_id
        }
    }
}
